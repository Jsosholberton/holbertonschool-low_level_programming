#include "main.h"
/**
 * _abs: print the valor absolute
 * @r: is the variable usade
 * return: -r if is negative
 * return: r if si positive
 *
 */
int _abs(int r)
{
if (r < 0)
{
  return (-r);
}
else
{
return (r);
}
}
