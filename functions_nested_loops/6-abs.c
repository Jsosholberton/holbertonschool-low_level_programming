#include "main.h"
/**
 * _abs - print the valor absolute.
 * @r: is the variable usade.
 *
 * Description: return the valor absolute
 * header: print the respose
 * Return: -r if is negative, return r if is positive, _abs return.
 */
int _abs(int r)
{
if (r <= 0)
{
return (-r);
}
else
{
return (r);
}
}
